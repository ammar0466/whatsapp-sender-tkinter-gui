# whatsapp-sender-tkinter-gui

simple gui for using baileys-api
send through csv file list
named csv wassap.csv on same directory

#bug
when in csv data have '&' it will break the code, in code need to find & esacape it
